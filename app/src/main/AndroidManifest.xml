<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="basico.android.cftic.edu.milinear">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivitySplitFernando"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/infinito"
            android:screenOrientation="portrait">

            <!-- para fijar la horientacion vertical -->

        </activity>
        <activity
            android:name=".BotonesActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LinearActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/original"
            android:screenOrientation="portrait" />
        <activity android:name=".SetNumHijosActivity" />
        <activity android:name=".SpinnerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NumberPickerActivity" />
        <activity android:name=".PauseActivity"></activity>
    </application>

</manifest>